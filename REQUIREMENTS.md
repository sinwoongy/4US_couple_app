# 4US 요구사항 문서 (초안)

> **상태:** 초안 v0.1 | **작성일:** 2026-07-07  
> 이 문서는 개발 착수 전 기준 문서입니다. 구현 과정에서 변경 시 버전을 올려 관리합니다.

---

## 1. 프로젝트 개요

### 1-1. 목적
커플 두 명이 함께한 데이트 경험을 기록하고 실시간으로 공유하는 웹 애플리케이션.

### 1-2. 사용자
- **대상:** 연인 관계의 두 명 (1:1 페어링)
- **플랫폼:** PC 웹브라우저 (Chrome / Edge 기준, 1280px 이상)
- **언어:** 한국어 UI

### 1-3. 핵심 가치
- 둘만의 공간 — 외부에 공개되지 않는 프라이빗 데이터
- 실시간 공유 — 한 명이 추가하면 상대방 화면에 즉시 반영
- 기억 보존 — 함께 간 곳, 먹은 것, 느낀 것을 영구 보존

---

## 2. 기술 스택 (확정)

| 역할 | 기술 |
|------|------|
| 프론트엔드 | React 18 + TypeScript + Vite |
| 스타일링 | Tailwind CSS |
| 인증 | Firebase Auth (Google 로그인 + 이메일) |
| DB | Firebase Firestore |
| 파일 저장 | Firebase Storage |
| 호스팅 | Firebase Hosting |
| 지도 렌더링 | Leaflet + OpenStreetMap |
| 장소 색칠 | GeoJSON (통계청 행정구역 데이터) |
| 캘린더 | FullCalendar (MIT 라이선스) |

---

## 3. 기능 요구사항

### F-01. 회원가입 / 로그인

| 항목 | 내용 |
|------|------|
| 지원 방식 | Google 소셜 로그인, 이메일+비밀번호 |
| 최초 가입 | 가입 후 닉네임 설정 화면으로 이동 |
| 로그인 유지 | 브라우저 세션 유지 (명시적 로그아웃 전까지) |
| 로그아웃 | 언제든지 가능 |

---

### F-02. 커플 연동 (페어링)

커플 앱의 모든 데이터는 페어링된 두 계정 사이에만 공유됩니다.

**흐름:**
1. 로그인 후 커플 미연결 상태이면 연동 화면 표시
2. **초대하기:** 초대 코드(6자리 영숫자) 생성 → 상대방에게 공유
3. **코드 입력:** 상대방이 코드 입력 → 두 계정이 같은 `coupleId`로 연결
4. 연결 완료 후 초대 코드는 즉시 무효화
5. 커플 연결 해제 기능 제공 (확인 팝업 필수)

**제약:**
- 1인당 최대 1개의 커플 연결만 허용
- 이미 연결된 상태에서 다른 코드 입력 불가

---

### F-03. 장소 기록

방문한 장소를 등록하고 목록으로 조회합니다.

**등록 항목:**
| 필드 | 타입 | 필수 | 설명 |
|------|------|------|------|
| 장소명 | 텍스트 | ✅ | 직접 입력 또는 지도 검색으로 자동 입력 |
| 카테고리 | 선택 | ✅ | 음식점, 카페, 관광지, 쇼핑, 기타 |
| 주소 | 텍스트 | ✅ | 지도 핀 선택 시 자동 입력 |
| 방문 날짜 | 날짜 | ✅ | 기본값: 오늘 |
| 사진 | 이미지 | ❌ | 최대 5장, 장당 최대 10MB |
| 메모 | 텍스트 | ❌ | 자유 텍스트 |
| 위도/경도 | 좌표 | ✅ | 지도 핀 선택 시 자동 저장 |

**목록 기능:**
- 등록된 장소 카드 형태로 표시
- 카테고리 필터, 날짜 정렬
- 장소 카드 클릭 시 상세 보기
- 두 사람 모두 장소 등록 가능 (등록자 표시)
- 본인이 등록한 장소만 수정/삭제 가능

---

### F-04. 지도 색칠

방문한 장소를 지도에 시각화합니다.

**지도 뷰:**
- 대한민국 전도를 기본 화면으로 표시 (Leaflet)
- 방문한 장소에 핀(마커) 표시
- 핀 클릭 시 장소명·방문 날짜 툴팁

**행정구역 색칠:**
- 방문한 장소가 속한 시·군·구 단위 행정구역을 색칠
- 방문 횟수에 따라 색 농도 차등 (1회: 연한 색 → 많을수록 진한 색)
- 색칠 데이터: 통계청 GeoJSON 활용
- 색칠된 구역 클릭 시 해당 지역 방문 장소 목록 표시

**통계:**
- 총 방문 장소 수
- 방문한 행정구역(시·군·구) 수 / 전체 비율

---

### F-05. 후기 작성

방문 장소에 대한 후기를 남깁니다.

**후기 항목:**
| 필드 | 타입 | 필수 | 설명 |
|------|------|------|------|
| 대상 장소 | 연결 | ✅ | 등록된 장소 중 선택 |
| 별점 | 1~5점 | ✅ | 반점 단위 (0.5 단위) |
| 텍스트 | 텍스트 | ❌ | 자유 후기 |
| 사진 | 이미지 | ❌ | 최대 10장 |
| 태그 | 다중 선택 | ❌ | #분위기좋음 #재방문의사 #가성비 등 |
| 작성자 | 자동 | ✅ | 본인 닉네임 자동 기록 |

**규칙:**
- 같은 장소에 두 사람이 각자 후기 작성 가능 (각자의 시각 반영)
- 본인 후기만 수정/삭제 가능
- 장소 상세 화면에서 두 사람의 후기를 나란히 표시

---

### F-06. 캘린더 / 일정 공유

데이트 일정을 등록하고 두 사람이 공유합니다.

**일정 항목:**
| 필드 | 타입 | 필수 | 설명 |
|------|------|------|------|
| 제목 | 텍스트 | ✅ | |
| 날짜 | 날짜 | ✅ | |
| 시간 | 시간 | ❌ | |
| 장소 | 텍스트 | ❌ | 등록된 장소 연결 또는 직접 입력 |
| 메모 | 텍스트 | ❌ | |
| 색상 | 선택 | ❌ | 일정 구분용 색상 라벨 |

**캘린더 기능:**
- 월간 뷰 기본 표시 (주간 뷰 전환 가능)
- 두 사람이 등록한 일정을 같은 캘린더에서 확인
- 일정 클릭 시 상세 보기
- 실시간 동기화: 한 명이 추가/수정/삭제하면 상대방 화면에 즉시 반영
- 본인이 등록한 일정만 수정/삭제 가능
- 오늘 날짜 강조 표시
- D-day 표시: 가장 가까운 미래 일정의 D-day를 메인 화면에 표시

---

### F-07. 메인 홈 화면

앱 진입 시 보이는 대시보드 역할.

**표시 요소:**
- 커플 닉네임 및 함께한 날 수 (D+N)
- 다음 일정 D-day
- 최근 등록 장소 (최신 3개)
- 이번 달 방문 횟수
- 지도 미리보기 (방문 핀 표시된 축소 지도)
- 빠른 장소 등록 버튼

---

## 4. 데이터 모델 (Firestore)

```
users/{userId}
  - nickname: string
  - profileImage: string (URL)
  - coupleId: string | null
  - createdAt: timestamp

couples/{coupleId}
  - members: [userId, userId]
  - createdAt: timestamp
  - inviteCode: string | null  ← 연결 완료 후 null

couples/{coupleId}/places/{placeId}
  - name: string
  - category: string
  - address: string
  - lat: number
  - lng: number
  - district: string          ← 행정구역명 (색칠용)
  - visitedAt: timestamp
  - createdBy: userId
  - photos: string[]          ← Storage URL 배열
  - memo: string
  - createdAt: timestamp

couples/{coupleId}/reviews/{reviewId}
  - placeId: string
  - rating: number            ← 1.0 ~ 5.0, 0.5 단위
  - text: string
  - photos: string[]
  - tags: string[]
  - author: userId
  - createdAt: timestamp
  - updatedAt: timestamp

couples/{coupleId}/events/{eventId}
  - title: string
  - date: string              ← YYYY-MM-DD
  - time: string | null       ← HH:mm
  - place: string
  - memo: string
  - color: string             ← hex 코드
  - createdBy: userId
  - createdAt: timestamp
  - updatedAt: timestamp

inviteCodes/{code}
  - coupleId: string
  - createdBy: userId
  - expiresAt: timestamp      ← 생성 후 24시간
  - used: boolean
```

---

## 5. Firebase 보안 규칙 (개요)

- `users/{userId}`: 본인만 읽기/쓰기
- `couples/{coupleId}/**`: `members` 배열에 포함된 두 userId만 읽기/쓰기
- `inviteCodes/{code}`: 로그인한 모든 사용자 읽기 가능, 생성은 본인만
- Storage: `couples/{coupleId}/` 경로는 해당 커플 멤버만 업로드/다운로드

---

## 6. 비기능 요구사항

| 항목 | 기준 |
|------|------|
| 초기 로딩 | 3초 이내 |
| 실시간 반영 | 상대방 변경사항 5초 이내 반영 |
| 이미지 업로드 | 장당 최대 10MB, 자동 WebP 변환 권장 |
| 브라우저 지원 | Chrome 최신, Edge 최신 |
| 해상도 | 1280px 이상 최적화 |
| 보안 | Firebase Security Rules로 커플 외 접근 원천 차단 |
| 데이터 백업 | Firebase 자동 백업 (유료 플랜 시 활성화 가능) |

---

## 7. 화면 목록

| 화면 ID | 화면명 | 설명 |
|---------|--------|------|
| S-01 | 로그인 | Google / 이메일 로그인 |
| S-02 | 커플 연동 | 초대 코드 생성·입력 |
| S-03 | 홈 | 대시보드 |
| S-04 | 지도 | 방문 지역 색칠 지도 |
| S-05 | 장소 목록 | 방문 장소 카드 목록 |
| S-06 | 장소 상세 | 장소 정보 + 두 사람 후기 |
| S-07 | 장소 등록/수정 | 지도 핀 선택 + 폼 |
| S-08 | 후기 작성/수정 | 별점 + 텍스트 + 사진 |
| S-09 | 캘린더 | 월간/주간 일정 뷰 |
| S-10 | 일정 등록/수정 | 일정 폼 |
| S-11 | 설정 | 닉네임 변경, 커플 연결 해제, 로그아웃 |

---

## 8. 개발 우선순위 (제안)

기능 간 의존 관계를 고려한 구현 순서:

```
1단계 (기반)      : F-01 (로그인) → F-02 (커플 연동)
2단계 (핵심)      : F-03 (장소 기록) → F-05 (후기 작성)
3단계 (시각화)    : F-04 (지도 색칠)
4단계 (일정)      : F-06 (캘린더)
5단계 (완성)      : F-07 (홈 대시보드)
```

---

## 변경 이력

| 버전 | 날짜 | 내용 |
|------|------|------|
| v0.1 | 2026-07-07 | 초안 작성 |
